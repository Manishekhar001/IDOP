fastapi>=0.115.0  # TestClient requires httpx<0.28 or fastapi>=0.115 with starlette>=0.40
uvicorn[standard]>=0.22.0
pydantic-settings>=2.0.0
langgraph>=0.1.0
qdrant-client>=1.7.0
langchain-core>=0.1.0
langchain-community>=0.0.30
langchain-text-splitters>=0.0.1
langchain-qdrant>=0.0.1
upstash-redis>=0.15.0
vanna>=0.2.0
pandas>=2.0.0
openpyxl>=3.1.0
psycopg[binary]>=3.1.0
psycopg2-binary>=2.9.9  # Provides psycopg2 module used by approval_gate, executor, vanna_service, etc.
boto3>=1.28.0
numpy>=1.24.0
python-docx>=1.0.0
python-dotenv>=1.0.0
loguru>=0.7.0
voyageai==0.3.7
langchain-voyageai==0.3.3
nomic>=3.5.3
langchain-nomic==1.0.1
langchain-groq==1.1.2
langchain-litellm==0.6.6
openai>=1.0.0  # Direct dependency for LLM judges (feature1_sql, feature2_mutation)
litellm==1.87.1
tavily-python>=0.3.0
langchain-tavily>=0.2.0  # Standalone Tavily integration (replaces langchain-community.tavily)
docling>=2.96.1  # py3-none-any wheel — Python 3.13 classifier confirmed
transformers>=4.42.0,<4.57.0  # docling-ibm-models needs AutoProcessor/AutoModelForImageTextToText in top-level namespace — removed in v4.57+ and v5+
opik>=2.0.54    # py3-none-any wheel — Python 3.13 classifier confirmed
moto[s3]>=5.0.0  # S3 storage backend mocking for tests
python-multipart>=0.0.12
langgraph-checkpoint-postgres>=3.0.5  # py3-none-any wheel — Python >= 3.10, 3.13 compatible
watchtower>=3.0.0
# Note: langgraph.store.postgres.aio (AsyncPostgresStore) is bundled in core langgraph —
# no separate langgraph-store-postgres package exists on PyPI.

pytest-asyncio>=1.4.0
